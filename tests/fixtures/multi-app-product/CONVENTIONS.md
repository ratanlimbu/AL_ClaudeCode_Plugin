# Conventions

Fictional fixture content.

- Mandatory prefix `FFS`, enforced by `AppSourceCop.json`.
- Object IDs are claimed in the table below in the same commit as the change that claims them.
- `Commit()` is forbidden outside the posting entry point — transaction boundaries belong to
  the caller.

| ID | Object |
|---|---|
| 50200 | `FFS Service Task` (table) |
| 50201 | `FFS Task Mgt.` (codeunit) |
